name: Simulated Jenkins

on:
  push:
    branches:
      - '**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Run Jenkinsfile
        run: |
          # TODO: Add Jenkinsfile contents here
